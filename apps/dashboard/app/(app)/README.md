# Authenticated tenant workspace

Staff routes for a single organization: bookings, resources, services,
customers, and settings. Tenant is resolved from the session, never from a
global switch in shared UI.
