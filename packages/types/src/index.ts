/**
 * Cross-application TypeScript contracts.
 *
 * Put types here only when more than one app needs the same shape
 * (for example API response DTOs consumed by dashboard and booking).
 *
 * Domain entities, value objects, and invariants stay in
 * `apps/api/src/modules/<domain>/domain`.
 */
export {};
