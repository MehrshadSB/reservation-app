# `@repo/config`

Shared runtime and build configuration.

Use this package for values that several apps must agree on: public URLs,
feature flags, and environment variable names. Do not put business rules
or booking capabilities here.

App-local Next.js config (`next.config.js`) stays in each app.
