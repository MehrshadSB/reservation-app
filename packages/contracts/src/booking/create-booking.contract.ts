/**
 * Request body for creating a generic booking.
 * Tour bookings with travelers use `@repo/contracts/travel`.
 * Domain Booking entity stays in `apps/api/src/modules/booking`.
 */
export type CreateBookingRequest = object;
