# `@repo/utils`

Small, pure helper functions shared across apps.

Only extract a helper here when it is genuinely generic. Booking duration
math, cancellation windows, and pricing calculations are domain logic and
stay in `apps/api`.
